from setuptools import setup

setup(
    name="lupusintabulabot",
    version="0.1",
    py_modules="lupusintabulabot",
    install_requires=[
        "pytest",
        "pytest-asyncio",
        "six",
        "transitions",
        ]
)
